{{-- Same page header shape as every other page: title + one-line description. --}}
<div class="mb-6 w-full">
    <flux:heading size="xl" level="1">{{ __('settings.heading') }}</flux:heading>
    <flux:text class="mt-1">{{ __('settings.subheading') }}</flux:text>
</div>
